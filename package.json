{
  "name": "omniwerx-io",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:survey": "SURVEY_ENABLED=true astro build",
    "build:no-survey": "SURVEY_ENABLED=false astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy --env production",
    "deploy:survey": "SURVEY_ENABLED=true astro build && wrangler deploy --env production",
    "deploy:no-survey": "SURVEY_ENABLED=false astro build && wrangler deploy --env production",
    "deploy:preview": "SURVEY_ENABLED=true astro build && wrangler deploy --env preview",
    "deploy:preview:no-survey": "SURVEY_ENABLED=false astro build && wrangler deploy --env preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.12",
    "astro": "^5.16.0",
    "he": "^1.2.0",
    "resend": "^6.5.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.3",
    "@types/he": "^1.2.3",
    "wrangler": "^4.51.0"
  }
}