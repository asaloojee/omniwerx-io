{
  "name": "",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "verify": "vp check && vp exec prettier --check \"**/*.astro\" && vp test --run --passWithNoTests && vp run build",
    "prepare": "vp config",
    "deploy": "pnpm run build && wrangler deploy --env production",
    "deploy:preview": "pnpm run build && wrangler deploy --env preview"
  },
  "dependencies": {
    "astro": "^7.2.0",
    "prettier": "^3.9.6",
    "prettier-plugin-astro": "^0.14.1",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "wrangler": "^4.103.0"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.16.0",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=22.18.0",
    "pnpm": ">=11 <12"
  }
}
